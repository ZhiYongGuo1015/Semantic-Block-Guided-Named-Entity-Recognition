# Code Availability

The code in this repository implements the semantic-block guided NER models and baseline models used in the manuscript.

Suggested manuscript statement:

```text
The source code used to train and evaluate the semantic-block guided geological named entity recognition models is available at [GitHub repository URL to be added upon acceptance or submission]. The annotated geological report dataset is not included in the code repository because it contains project-specific report text; the repository provides the required BIO data format and reproduction scripts.
```

Before submission, replace the bracketed GitHub URL with the actual repository URL.

If the journal requires data availability and code availability to be separated, keep the code statement above under `Code Availability` and describe the dataset access route separately under `Data Availability`.

